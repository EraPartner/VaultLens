# Generated by tools/agents/generate-adapters.py; do not edit.
name = "wiki-project-runner"
description = "Autonomous nightly project runner. For one opted-in project, grooms its AGENDA.md Inbox into structured Tasks, executes the tasks that are 100% clear and due, files clarifications for anything ambiguous, marks tasks needing a non-allowlisted host as blocked, and advances recurrence state. Writes only inside projects/<slug>/; edits for real but never commits."
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "Before doing any other work, read `.agents/roles/wiki-project-runner.md` in full and follow it as your canonical role instructions. Do not delegate or spawn another agent. The role's file scope is an instruction boundary, not proof of hard isolation: a parent Codex session can override this custom agent's sandbox default. For hard isolation, run wiki-agent.py through the matching container profile."
