# Brain project-specific egress extras (merged onto base-allowlist.txt by
# devcontainer-egress/sync.sh into the baked allowlist.txt).
#
# NOT here on purpose:
#   - ollama.com / registry.ollama.ai — inference goes to the HOST ollama daemon
#     via host.docker.internal:11434 (a direct firewall hole, not this proxy).
#   - huggingface.co — qmd's embedding models are seeded from the host cache, so
#     they are never downloaded. Add huggingface.co + cdn-lfs.huggingface.co and
#     rebuild only if you switch to in-container `qmd embed` downloads.

# Anthropic / Claude Code (auth, auto-updater, docs beyond the base API).
platform.claude.com
claude.ai
code.claude.com
docs.claude.com
downloads.claude.ai

# npm (installs qmd / copilot / opencode / safe-chain at runtime)
registry.npmjs.org

# GitHub container registry (releases / images).
ghcr.io
pkg-containers.githubusercontent.com

# GitHub Copilot (used by both the `copilot` CLI and opencode's
# github-copilot/gpt-5.2 provider). github.com + api.github.com (base) cover the
# OAuth/login path; .githubusercontent.com (base) covers the proxy/telemetry CDN.
.githubcopilot.com
collector.github.com
default.exp-tas.com

# opencode (self-update + platform binary download on npm install)
opencode.ai

# safe-chain malware list (supply-chain screening of npm/pip installs)
malware-list.aikido.dev

# Plugins: context7 MCP (npx @upstash/context7-mcp → context7 docs API)
.context7.com
